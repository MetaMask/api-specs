{
    "name": "@metamask/metamask-api-specs",
    "description": "",
    "scripts": {
        "build": "mkdir -p build && node merge-openrpc.js && openrpc-cli bundle GENERATED-openrpc.json > build/metamask-openrpc.json && rm GENERATED-openrpc.json",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "Apache-2.0",
    "devDependencies": {
        "@xops.net/openrpc-cli": "^1.0.2"
    },
    "dependencies": {
        "node-fetch": "^2.6.1"
    },
    "version": "0.0.2"
}
